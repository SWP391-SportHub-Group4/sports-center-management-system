namespace SportHub.Identity;

// Marker rỗng — chỉ dùng làm anchor cho ModelBuilder.ApplyConfigurationsFromAssembly
// (SportHubDbContext, mục 9), không có logic gì bên trong.
public sealed class IdentityModuleMarker;
